[pytest]
testpaths = tests
# Put the project root on sys.path so `import src` works regardless of how
# pytest is invoked (bare `pytest` console script or `python -m pytest`).
pythonpath = .
addopts = -q
