[pytest]
testpaths = tests
addopts =
    -q
    --cov=agents
    --cov=gateway
    --cov=services
    --cov=integrator
    --cov=mcp
    --cov-report=term-missing
    --cov-report=json:coverage.json
filterwarnings =
    ignore::DeprecationWarning
