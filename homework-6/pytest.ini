[pytest]
testpaths = tests
addopts =
    -q
    --cov=agents
    --cov=integrator
    --cov=mcp
    --cov-report=term-missing
    --cov-report=json:coverage.json
filterwarnings =
    ignore::DeprecationWarning
